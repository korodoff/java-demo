Prototypes:
The prototype is an object that is associated with every functions and objects.
Prototypes are the mechanism by which JavaScript objects inherit features from one another.
Prototype is a global property which is available with almost all the objects
it is like object but it contains methods and the methods can also accept by child object
prtotoype  inherit features from one another.
mechanism by which js object inherit features frm other
Prototype provides various functions for developing JavaScript applications.


